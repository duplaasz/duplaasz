{
  "routes": [
    {
      "route": "/_teszt",
      "redirect": "/kapcsolat",
      "statusCode": 302
    },
    {
      "route": "/{*path}.{ext}",
      "rewrite": "/{*path}.{ext}"
    },
    {
      "route": "/{*path}",
      "rewrite": "/{*path}.html"
    }
  ],
  "responseOverrides": {
    "404": { "rewrite": "/404.html" }
  }
}
